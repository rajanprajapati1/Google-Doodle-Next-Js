import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Diwali Google Doodle Clone",
  description: "A festive tribute to Diwali, inspired by Google's Doodle. Celebrate the festival of lights with interactive animations and joyful visuals.",
  keywords: "Diwali, Google Doodle, interactive, festival of lights, animations",
  author: "Rajan Prajapati",
};


export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
